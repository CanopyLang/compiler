module TestForeignSyntax exposing (main)

import Html exposing (Html, div, text)

-- Test the exact foreign import syntax from the parser
foreign import javascript "external/simple.js" as SimpleFFI

main : Html msg
main =
    div []
        [ text "Testing foreign import syntax"
        ]