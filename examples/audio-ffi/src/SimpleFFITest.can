module SimpleFFITest exposing (main)

import Html exposing (Html, div, text)

-- Test simple foreign import syntax
foreign import javascript "external/simple.js" as SimpleJS

main : Html msg
main =
    div []
        [ text "Simple FFI Test"
        , text (SimpleJS.getMessage)
        ]