module SimpleDebug exposing (main)

-- Simple test to isolate the FFI type issue

type UserActivated
    = Click
    | Keypress

foreign import javascript "debug_simple.js" as DebugFFI

testFunction : UserActivated
testFunction =
    DebugFFI.getActivation

main : UserActivated
main = testFunction