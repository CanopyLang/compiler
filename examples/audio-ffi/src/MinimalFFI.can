ffi module MinimalFFI exposing (main)

import Html exposing (Html, div, text)

-- Simple type defined before foreign import
type Wrapper = Wrapper String

-- Foreign import
foreign import javascript "external/simple.js" as Simple

-- Simple main function
main : Html msg
main =
    div [] [ text "Minimal FFI test" ]