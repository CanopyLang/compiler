module MinimalComplexFFI exposing (main)

import Html exposing (Html, div, text, p, h1)
import Html.Attributes exposing (style)

-- Define one complex type
type UserActivated = Click | Keypress | Touch

-- Import FFI exactly like math-ffi
foreign import javascript "external/math.js" as Math

main : Html Never
main =
    div [ style "padding" "20px" ]
        [ h1 [] [ text "Minimal Complex FFI Test" ]
        , p [] [ text "Testing one complex type" ]
        ]