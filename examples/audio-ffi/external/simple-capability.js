/**
 * Simple capability FFI for testing
 */

/**
 * Consume user activation and detect gesture type
 * @canopy-type UserActivated
 * @name consumeUserActivation
 */
function consumeUserActivation() {
    return { $: 'Click' };
}