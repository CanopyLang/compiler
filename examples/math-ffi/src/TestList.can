module TestList exposing (main)

import Html exposing (Html, text, div)
import String

main : Html msg
main =
    let
        numbers = [1, 2, 3, 4, 5]
        count = List.length numbers
    in
    div [] [ text (String.fromInt count ++ " items") ]