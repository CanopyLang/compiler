module Main exposing (main)

foreign import javascript "test-void-direct.js" as TestFFI

main =
    let
        -- Test A: Direct call without assignment
        _ = TestFFI.voidFunction 42
    in
    ()
