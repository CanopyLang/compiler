module Main exposing (main)

import Browser
import Html exposing (Html, text)

main : Program () () ()
main =
    Browser.sandbox
        { init = ()
        , update = \_ model -> model
        , view = \_ -> text "Hello, Canopy!"
        }
