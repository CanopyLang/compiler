{
  "name": "@canopy/test",
  "version": "1.0.0",
  "description": "Browser testing runtime dependencies for canopy/test",
  "private": true,
  "dependencies": {
    "playwright": "^1.40.0",
    "@axe-core/playwright": "^4.8.0",
    "pixelmatch": "^5.3.0",
    "pngjs": "^7.0.0"
  },
  "devDependencies": {},
  "scripts": {
    "install-browsers": "npx playwright install",
    "postinstall": "npx playwright install chromium"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
