/*

*/


// CAPABILITY GRANT
//
// Capabilities are phantom types -- they carry no runtime information.
// The `grant` value is shared by all capability tokens because the
// permission enforcement happens at compile time (via the type system
// and build-time canopy.json validation), not at runtime.

var _Capability_grant = 0;
