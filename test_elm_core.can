module TestElmCore exposing (main)

import List
import Basics
import Maybe

main =
    case Maybe.Just 42 of
        Maybe.Nothing -> List.length []
        Maybe.Just x -> List.length [x] + Basics.identity 1