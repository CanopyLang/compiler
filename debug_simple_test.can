module SimpleDebug exposing (main)

-- Simple test to isolate the FFI type issue

type UserActivated
    = Click
    | Keypress

foreign import javascript "debug_simple.js" as SimpleFFI

testFunction : UserActivated
testFunction =
    SimpleFFI.getActivation

main : UserActivated
main = testFunction