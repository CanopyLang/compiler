module DebugArrayTest exposing (..)

import Array

testFunction : Array.Array Int
testFunction =
    let
        builder = Array.empty |> Array.push 1 |> Array.push 2
    in
    Array.fromList [1, 2, 3]